---
rails_env: production
secret_key_base: "{{ vaulted_secret_key_base }}"

# TODO: Move to seperate server main_db_server: 159.203.30.99
main_db_server: localhost

main_web_server: 159.203.14.69
server_name: "{{ main_web_server }}"

db_rails_username: rails
db_rails_password: "{{ vaulted_db_rails_password }}"
db_rails_port: 5432
db_rails_name: workshop_app